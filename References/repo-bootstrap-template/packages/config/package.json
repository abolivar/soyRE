{
  "name": "@brokerops/config",
  "version": "0.1.0",
  "private": true,
  "main": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "echo 'No lint configured yet for config package'",
    "test": "echo 'No tests configured yet for config package'",
    "build": "echo 'No build required yet for config package'"
  }
}
